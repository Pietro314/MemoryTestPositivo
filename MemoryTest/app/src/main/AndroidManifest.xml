<?xml version="1.0" encoding="utf-8"?>
<!--
    sharedUserId="android.uid.system": faz o APK rodar com uid 1000 (system).
    Necessario pra memtester ter mlock unlimited via CAP_SYS_RESOURCE.

    Atencao: requer fresh install (factory reset ou flash de imagem nova).
    Nao pode ser "upgrade" sobre uma versao anterior do APK que rodava
    como app comum — Android proibe troca de uid e da INSTALL_FAILED_UID_CHANGED.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    android:sharedUserId="android.uid.system">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".MemoryTestApp"
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".ui.dashboard.DashboardActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.devicelist.DeviceListActivity"
            android:exported="false"
            android:windowSoftInputMode="stateHidden" />

        <activity
            android:name=".ui.deviceedit.DeviceEditActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize|stateHidden" />

        <activity
            android:name=".ui.devicedetail.DeviceDetailActivity"
            android:exported="false" />

        <activity
            android:name=".ui.testrun.TestRunActivity"
            android:exported="false"
            android:keepScreenOn="true" />

        <activity
            android:name=".ui.history.HistoryActivity"
            android:exported="false" />

        <activity
            android:name=".ui.history.RunDetailActivity"
            android:exported="false" />

        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize|stateHidden" />

        <!--
            AutoTestActivity: disparada por outro app (ex: app da fabrica) via:
              Intent("com.factory.memorytest.action.AUTO_TEST")
                  .setPackage("com.factory.memorytest")
            Le memorytestconfig.json do disco, identifica device+ramGb e roda
            factory + deep em sequencia.
            Sem permission custom (decisao: qualquer app pode disparar).
        -->
        <activity
            android:name=".ui.autotest.AutoTestActivity"
            android:exported="true"
            android:keepScreenOn="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|screenLayout|locale|fontScale|keyboardHidden">
            <intent-filter>
                <action android:name="com.factory.memorytest.action.AUTO_TEST" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
